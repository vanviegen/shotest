{
  "name": "shotest-test-project",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:run",
    "test:run": "rm -rf test-results && SHOTEST_CAPTURE_HTML=false SHOTEST_DEMO=off shotest test",
    "accept": "rm -rf test-accepted && node scripts/copy-accepted.mjs",
    "postinstall": "node scripts/link-shotest.mjs"
  },
  "devDependencies": {
    "shotest": "file:.."
  }
}
